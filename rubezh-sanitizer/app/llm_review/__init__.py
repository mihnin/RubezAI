"""LLM-review — фильтр 2/3: смысловая подсказка рисков локальной LLM.

Модуль подключается через интерфейс ``Detector`` и **дополняет**
детерминированные детекторы (фильтр 1), а не заменяет их и **не принимает**
решений allow/deny — это остаётся за policy engine (см. CLAUDE.md).
"""
